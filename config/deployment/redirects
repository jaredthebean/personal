# | Redirection Path | S3 Object Path |
# Redirection Path: The path that the requested 'object path' should be
#   redirected to. Note: this should be URL encoded as it is used verbatim in
#   HTTP redirects
# S3 Object Path: The S3 path of the object that should redirect to the
#   Redirection Path. An empty (0-byte) object will be uploaded to the 
#   S3 bucket and the Redirection Path will be attached to it as metadata.
/r%C3%A9sum%C3%A9/	/resume/index.html
